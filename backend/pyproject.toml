[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# E/F = pyflakes + pycodestyle core rules, I = isort (import sorting),
# UP = pyupgrade (modern syntax), B = bugbear (common bug patterns)
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"